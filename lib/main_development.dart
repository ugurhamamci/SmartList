import 'package:smartlist/app.dart';
import 'package:smartlist/core/bootstrap.dart';

/// Development entry point. Run with `--dart-define=FLAVOR=development`.
Future<void> main() => bootstrap(SmartListApp.new);
